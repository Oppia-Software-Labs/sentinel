/**
 * Unprotected agent loop — Fabián: point at OZ Relayer (no ShieldPay).
 * Stub until wired to the hosted x402 facilitator URL (see OZ_RELAYER_URL / docs)
 */
console.info('loop-agent: stub — implement 20 × $2.50 direct to OZ Relayer')
