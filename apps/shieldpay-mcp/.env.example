# Your Sentinel API key — get it at https://app.sentinel.xyz/dashboard/settings
SENTINEL_API_KEY=sk_live_...

# Optional: override API URL (defaults to https://api.sentinel.xyz)
# SENTINEL_API_URL=https://api.sentinel.xyz

# Optional: how Claude identifies itself in transactions (default: claude-code)
# SENTINEL_AGENT_ID=claude-code
